#include <stdio.h>
main()
{
	int j = 024, k = 24, L = 0x24, hap;
	hap = j + k + L;
	printf("%d, %d, %d, %d\n", j, k, L, hap);
}
// 024 == 20 8진수로서 (8^1 * 2) + (8^0 * 4) = 16 + 4 = 20
// 0x24 == 36 16진수로서 (16^1 * 2) + (16^0 + 4) =  32 + 4 = 36